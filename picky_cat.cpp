#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    int n;
    cin >> n;

    vector<int> v(n);
    for (int i = 0; i < n; i++) cin >> v[i];

    int midx = ceil(n / 2);
    int exp = v[0];

    // make exp the 2nd midx element of the sorted array



    return 0;
}