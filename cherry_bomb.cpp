#include<bits/stdc++.h>
using namespace std;

void solve() {
    int n, m, q;
    cin >> n >> m >> q;
    vector<int> b(m), a(q);
    for (int i = 0; i < m; i++) cin >> b[i];
    for (int i = 0; i < q; i++) cin >> a[i];
}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}