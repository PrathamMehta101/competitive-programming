#include<bits/stdc++.h>
#define ll long long 
using namespace std;

int coins = 0;

int main() {
    ll t;
    cin >> t;
    while (t--)
    {
        int t;
        cin >> t;
        while (t--) {
            int n;
            cin >> n;
            vector<int> v(n);
            for (int i = 0; i < n; i++) cin >> v[i];
        }
    }

    return 0;
}