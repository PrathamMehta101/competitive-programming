#include<bits/stdc++.h>
using namespace std;

void solve() {
    int a, b, x, y;
    cin >> a >> b >> x >> y;

    if (a > b) {
        cout << -1 << endl;
        return;
    }

}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
    return 0;
}